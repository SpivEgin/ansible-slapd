---
- hosts: all
  remote_user: root
  package:
    name: {{ item }}
    state: {{ present }}
  become: true
  with_items:
    - lsof
    - openssl
    
- hosts: all
  remote_user: root
  roles:
    - role_under_test
